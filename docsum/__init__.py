"""docsum — summarize documents that are too large for an LLM's context window."""

# The version this code reports. The declared copy of the same number is the VERSION file
# at the repo root: the gate's `artifact identity` stage and tests/test_version_contract.py
# both fail when the two disagree, so bump both in one edit.
__version__ = "1.0.0"
